<section class="calendar">
  {{#if message}}
    <h1>{{message}}</h1>
  {{/if}}
  {{#if script}}
    <div id="left"></div>
    <div id="right"></div>
    <script src="{{script}}"></script>
  {{/if}}
</section>